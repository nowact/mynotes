0. 先定位
   valgrind等性能检查工具
   linux /proc/<pid> 目录下的统计数据

1. 减少内存拷贝
   C++11 std::vector emplace
   io分散读、集中写

2. 减少CPU Cache失效
   减少远端内存访问
   提高CPU分支预取判断
   分支预测指令
   二维数组先取行再取列

3. 线程的使用
   多线程并发充分利用多核资源
   避免过多加锁，防止拖累并发
   避免线程过多，减少线程调度开销
   线程绑定cpu core

